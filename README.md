# Random password_Generator
A simple Python tool to generate secure random passwords with customizable options .

# Features 
- Generate random passwords
- Customize password length
- Include | Exclude / special symboles
- Include | Exclude / numbers
- Error handling

# How to run
```bash
python generate_password.py
#Example
Random Password Generator
==============================
Enter password length (default 12):10
Include special symbols ? (y/n , default y):y
Include numbers ? (y/n , default y):y

 Generated_password: v.$i{"%u&A
Length: 10 characters

# Technologies Used
.Python 3.x
.random library
.string library

# Developer
**Omar AL 3ngery**-
